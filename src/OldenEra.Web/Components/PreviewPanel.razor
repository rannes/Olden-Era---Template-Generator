@* Renders the preview PNG (or a placeholder) and exposes Generate/Download buttons.
   Wrapped in <details> so narrow viewports can collapse the preview and reach
   the action bar without scrolling past a tall image. Open by default; the
   summary is visually hidden on desktop via CSS. *@

<details class="oe-preview-details" open>
    <summary class="oe-preview-summary">Preview</summary>
    <div class="oe-preview">
        @if (PngBytes is { Length: > 0 })
        {
            <img src="@($"data:image/png;base64,{Convert.ToBase64String(PngBytes)}")" alt="Map preview" />
        }
        else
        {
            <span class="placeholder">Preview will appear after Generate.</span>
        }
    </div>
</details>

@code {
    [Parameter] public byte[]? PngBytes { get; set; }
}
