<Window x:Class="OldenEra.TemplateEditor.Views.PresetNameDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Save Preset"
        Width="380" Height="160"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="Name for this preset:"
                   Margin="0,0,0,6" />

        <TextBox x:Name="TxtName"
                 Grid.Row="1"
                 MaxLength="64"
                 Padding="4,2" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            <Button x:Name="BtnOk"
                    Content="Save"
                    MinWidth="80"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="BtnOk_Click" />
            <Button x:Name="BtnCancel"
                    Content="Cancel"
                    MinWidth="80"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>
